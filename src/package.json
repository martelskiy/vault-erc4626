{
  "name": "vault",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "hardhat clean & forge clean",
    "lint": "solhint list-rules && solhint contracts/**/*.sol test/**/*.sol",
    "format": "prettier --write contracts/**/*.sol test/**/*.sol",
    "deploy": "hardhat run .\\scripts\\main.ts",
    "compile": "forge build --build-info",
    "test": "hardhat test --typecheck & forge test -vv",
    "node": "hardhat node"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-foundry": "^1.1.1",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv-defaults": "^5.0.2",
    "prettier": "^3.2.5",
    "prettier-plugin-solidity": "^1.1.3",
    "solhint": "^4.0.0",
    "solhint-plugin-prettier": "^0.1.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "@openzeppelin/contracts-upgradeable": "^5.0.1"
  }
}
